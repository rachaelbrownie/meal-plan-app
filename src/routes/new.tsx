import { Form } from "react-router-dom";

export const New = () => {
  return (
    <div>
      <h2>New meal plan</h2>
      <Form>{`<==== Form to generate new menu goes here ====>`}</Form>
    </div>
  );
};
